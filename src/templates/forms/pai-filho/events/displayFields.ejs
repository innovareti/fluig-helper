function displayFields(form, customHTML) {

    var atividadeAtual = getValue("WKNumState")
    form.setValue("atividade", atividade)

    const atividade = {
        inicio: 0,
        inicioRetorno: 4,
        aprovacao: 5
    }

    if ( atividadeAtual == atividade.inicio || atividadeAtual == atividade.inicioRetorno ) {

    }
    
    if ( atividadeAtual == atividade.aprovacao) {
        
    }

}